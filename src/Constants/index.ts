export enum Routes {
  Root = "/",
  ListaDeProductos = "/list/Productos",
  ListaPedidos = "/pedido",
}

export enum LocalStorageKeys {
  Listapedido = "listapedido",
  Pedido = "pedido",
}

export enum AboutWeb {
  Footer = "© 2023 OmerSolutions",
}
